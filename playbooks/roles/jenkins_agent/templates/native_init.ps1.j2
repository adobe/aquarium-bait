#!powershell
# Script runs jenkins agent in native environment

$env:JAVA_HOME="${env:ENV_PATH}{{ java_home | replace('/', '\\') }}"
$env:CONFIG_URL="{{ config_url }}"

& "${env:ENV_PATH}{{ script_dir | replace('/', '\\') }}\jenkins_agent.ps1"
